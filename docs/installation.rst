============
Installation
============

At the command line::

    $ easy_install django-coinpayments

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-coinpayments
    $ pip install django-coinpayments
